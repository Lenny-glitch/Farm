// Template. Copy this file to `firebase-config.js` (gitignored) and fill in
// real values from the Firebase console: Project settings > General > Your
// apps > SDK setup and configuration.
//
// NEVER commit `firebase-config.js` with real keys — see PROJECT_STATE.md.
export const firebaseConfig = {
  apiKey: 'YOUR_API_KEY',
  authDomain: 'YOUR_PROJECT_ID.firebaseapp.com',
  projectId: 'YOUR_PROJECT_ID',
  storageBucket: 'YOUR_PROJECT_ID.firebasestorage.app',
  messagingSenderId: 'YOUR_SENDER_ID',
  appId: 'YOUR_APP_ID'
}
